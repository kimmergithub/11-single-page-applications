'use strict';
var app = app || {};

(function(module) {
  const aboutController = {};

  // TODO-WORKING: Define a function that hides all main section elements, and then reveals just the #about section:
  // JQuery === aboutController.init =>  ====> .init?
  // we want to hide #articles and show #about

  module.aboutController = aboutController;
})(app);
